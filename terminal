======================================= firstplato.com ======
= List terminal command :
= installing git
# apt-get install git
= setup user and email for git
# git config --global user.name "Your Name"
# git config --global user.email "your@email.com"
= save git credential
# git config credential.helper store
= install debian based package
# dpkg -i file.deb
= if error dependencies along install process
# apt --fix-broken install
= and or
# apt --fix-missing install
= install retropie
= clone retropie from github
# sudo apt-get install -y git dialog unzip xmlstarlet
# git clone --depth=1 https://github.com/RetroPie/RetroPie-Setup.git
# cd RetroPie-Setup
# nano /home/track/RetroPie-Setup/scriptmodules/
= Find Raspbian|Debian, change to be Raspbian|Debian|MX and save
sudo ./retropie_setup.sh
= install cmus
# sudo apt install cmus
= run cmus and use 5 key, to browse audio files, and use a key to add it in music player
= install cava
# apt-get install libfftw3-dev libasound2-dev libncursesw5-dev libpulse-dev libtool
# git clone git://github.com/karlstav/cava
# cd cava/
# ./autogen.sh
# ./configure
# make
# sudo make install
# cava
= install adb + fastboot
# sudo apt-get install android-tools-adb
# sudo apt-get install android-tools-fastboot
# sudo adb start-server
# sudo adb devices
# sudo adb kill-server
=============================================================
